{
  "name": "second-brain",
  "version": "0.1.0",
  "private": true,
  "description": "A universal knowledge capture system powered by a Telegram bot, Vercel functions, and Supabase",
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "@vercel/node": "^5.1.5",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
